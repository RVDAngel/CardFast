<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog_rounded"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Título o nombre de la tarjeta -->
        <TextView
            android:id="@+id/txtTituloDetalle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/titulo_tarjeta"
            android:textSize="20sp"
            android:textStyle="bold"
            style="@style/TEXT"
            android:layout_marginBottom="8dp" />

        <!-- Código interno de la tarjeta -->
        <TextView
            android:id="@+id/txtCodigoDetalle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/codigo_tarjeta"
            android:textSize="16sp"
            style="@style/TEXT"
            android:layout_marginBottom="16dp" />

        <!-- Saldo -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/txtSaldoDetalle"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/saldo"
                android:textSize="18sp"
                android:textStyle="bold"
                style="@style/TEXT" />

            <ImageButton
                android:id="@+id/btnToggleSaldo"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_visibility"
                android:contentDescription="@string/mostrar_ocultar_saldo"
                android:padding="4dp" />
        </LinearLayout>

        <!-- Botón para recargar saldo -->
        <Button
            android:id="@+id/btnRecargarSaldo"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/txtRecarga"
            android:background="@drawable/btn_rounded"
            android:textColor="#FFFFFF"
            android:textAllCaps="false"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>
</ScrollView>
